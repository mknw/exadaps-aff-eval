# Core pipeline
datasets>=2.19.0
pandas>=2.2.0
pyarrow>=16.0.0
structlog>=24.0.0
click>=8.1.7
Pillow>=10.3.0
pymupdf>=1.24.0
pypdf>=4.0.0
reportlab>=4.2.0
requests>=2.31.0

# Optional — Genalog degradation (graceful fallback if unavailable)
# genalog>=0.1.0

# Dev / test
pytest>=8.2.0
pytest-cov>=5.0.0
ruff>=0.4.0
